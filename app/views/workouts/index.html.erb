<h1>Workouts</h1>

<%= link_to "Create New Workout", new_workout_path %>

<% @workouts.each do |workout| %>
<ul>
  <li><%= link_to "#{workout.title}", workout_path(workout) %>
  <%= link_to "Delete", workout_path(workout), method: :delete %></li>
</ul>
<% end %>
